upstream backend_server {
    server backend:8000;
}

resolver 127.0.0.11 valid=10s ipv6=off;

server {
    listen 80 default_server;
    server_name svitpc.com.ua www.svitpc.com.ua _;
    client_max_body_size 50M;

    location /static/ {
        alias /app/staticfiles/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # Не хешовані ES-модулі (import "./modules/x.js" зсередини app.js) — той
    # самий URL живе назавжди, тож immutable вище кешував би стару версію
    # на 30д без способу форсувати оновлення. Див. docker.prod.conf.
    location /static/js/modules/ {
        alias /app/staticfiles/js/modules/;
        add_header Cache-Control "no-cache";
    }

    location /media/ {
        alias /app/media/;
        expires 7d;
    }

    location / {
        set $upstream http://backend:8000;
        proxy_pass $upstream;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_redirect off;
        proxy_read_timeout 120s;
    }
}
