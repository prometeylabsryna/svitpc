from django.apps import AppConfig

class IntegrationConfig(AppConfig):
    name = 'apps.integrations.brain'
    verbose_name = 'brain Integration'
    default_auto_field = 'django.db.models.BigAutoField'
