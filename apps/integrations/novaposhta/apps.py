from django.apps import AppConfig

class IntegrationConfig(AppConfig):
    name = 'apps.integrations.novaposhta'
    verbose_name = 'novaposhta Integration'
    default_auto_field = 'django.db.models.BigAutoField'
