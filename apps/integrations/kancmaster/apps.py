from django.apps import AppConfig

class IntegrationConfig(AppConfig):
    name = 'apps.integrations.kancmaster'
    verbose_name = "Kancmaster Integration"
    default_auto_field = 'django.db.models.BigAutoField'
