from django.apps import AppConfig

class IntegrationConfig(AppConfig):
    name = 'apps.integrations.vchasnokasa'
    verbose_name = 'vchasnokasa Integration'
    default_auto_field = 'django.db.models.BigAutoField'
