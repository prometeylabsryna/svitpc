from django.apps import AppConfig

class IntegrationConfig(AppConfig):
    name = 'apps.integrations.ukrposhta'
    verbose_name = 'ukrposhta Integration'
    default_auto_field = 'django.db.models.BigAutoField'
